# Strixlog

The repository contains a simple log aggregator and analyzer. The app contains a dashboard to review the logs. Furthermore for testing a small log generator app exists.

## Folder Structure

- strixlog -- The main application
- randomlog -- A small app that generates random logs, which can be modified by and rest api
